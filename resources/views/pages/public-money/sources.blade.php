<x-layouts.site :title="__('ui.public_money.sources_method').' | '.config('app.name')">
    <h1 class="text-4xl font-black">{{ __('ui.public_money.sources_method') }}</h1><p class="mt-4 max-w-3xl leading-8 text-ink-muted">{{ __('ui.public_money.methodology') }}</p>
    <div class="mt-8 grid gap-4 lg:grid-cols-2">@foreach ($sources as $source)<article class="rounded-3xl border border-line bg-surface p-6"><div class="flex items-start justify-between gap-3"><h2 class="text-xl font-black">{{ app()->isLocale('ar') ? $source->name_ar : $source->name_en }}</h2><span class="h-3 w-3 shrink-0 rounded-full {{ $source->last_error_at && (!$source->last_success_at || $source->last_error_at->gt($source->last_success_at)) ? 'bg-red-500' : 'bg-emerald-500' }}"></span></div><p class="mt-3 text-sm text-ink-muted">{{ __('ui.public_money.last_success') }}: {{ optional($source->last_success_at)->format('Y-m-d H:i') ?: __('ui.public_money.not_yet') }}</p><p class="mt-1 text-sm text-ink-muted">{{ __('ui.public_money.frequency') }}: {{ __('ui.public_money.daily') }}</p><a href="{{ $source->discovery_url }}" target="_blank" rel="noopener noreferrer" class="mt-5 inline-flex font-black text-accent">{{ __('ui.public_money.open_official_source') }}</a></article>@endforeach</div>
</x-layouts.site>
