<x-layouts.site :title="$report->title_ar.' | '.config('app.name')"><article class="mx-auto max-w-4xl rounded-[2rem] border border-line bg-surface p-6 sm:p-10"><p class="text-sm font-black text-accent">{{ __('ui.public_money.analysis') }}</p><h1 class="mt-2 text-4xl font-black">{{ app()->isLocale('ar') ? $report->title_ar : ($report->title_en ?: $report->title_ar) }}</h1><div class="prose mt-8 max-w-none">{!! app()->isLocale('ar') ? $report->body_ar : ($report->body_en ?: $report->body_ar) !!}</div>@if ($report->evidence_links)<h2 class="mt-10 text-xl font-black">{{ __('ui.public_money.evidence') }}</h2><ul class="mt-3 space-y-2">@foreach ($report->evidence_links as $link)<li><a class="text-accent" href="{{ $link }}" target="_blank" rel="noopener noreferrer">{{ $link }}</a></li>@endforeach</ul>@endif</article></x-layouts.site>
